<Image
  src={project.thumbnail_url.startsWith('/') ? project.thumbnail_url : `/${project.thumbnail_url}`}
  alt={project.title}
  width={800}
  height={400}
  className="w-full h-48 object-cover"
  unoptimized
/>
